{
  "name": "my-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",                 
    "export": "next build",                
    "clean": "rm -rf .next out",
    "deploy": "npm run build && touch out/.nojekyll && rm -rf _next && cp -R out/. . && rm -rf out && git add -A && git commit -m 'Deploy updated site' && git push origin main"
  },
  "dependencies": {
    "@heroicons/react": "^2.1.5",
    "lodash.throttle": "^4.1.1",
    "next": "^15.0.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-parallax-tilt": "^1.7.242",
    "styled-jsx": "^5.1.0",
    "three": "^0.169.0",
    "three-stdlib": "^2.33.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.13"
  }
}
