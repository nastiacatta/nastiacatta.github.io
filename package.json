{
  "name": "my-portfolio",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "deploy": "next build && touch out/.nojekyll && cp -r out/* . && rm -rf out && git add . && git commit -m 'Deploy to GitHub Pages' && git push origin main"
  },
  "dependencies": {
    "autoprefixer": "^10.4.20",
    "next": "^14.2.13",
    "postcss": "^8.4.47",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tailwindcss": "^3.4.12"
  },
  "devDependencies": {}
}
